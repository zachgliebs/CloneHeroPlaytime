Run the code with
```
node clone-hero-playtime.js /path/to/logs
```
path to logs can be found by going to Clone Hero, navigating to "Settings", going to "General", and scrolling down to "Open Log Folder" or a similarly named button. Then just copy the path to the folder and run the code with it as mentioned above. 
